"""The trusted verification-policy model (Phase 1.2a).

A requirement is keyed by CHECK IDENTITY. It says: the check named
``<identity>`` must produce a valid, satisfactory result bound to this
artifact, target and verification attempt. Which verifier implementations may
satisfy it is a separate, policy-named fact — see :mod:`.snapshot`.
"""
