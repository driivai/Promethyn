"""Measurement harnesses that ship with the runtime.

Everything here is read-only with respect to runtime state: a measurement never
creates calibration samples, trust rows, or ledger entries. Evaluation is not
experience.
"""
