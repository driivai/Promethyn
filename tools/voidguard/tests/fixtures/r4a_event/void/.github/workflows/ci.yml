name: ci
on: [push]
jobs:
  release:
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    steps:
      - run: echo release
  gated:
    runs-on: ubuntu-latest
    steps:
      - name: deploy
        if: secrets.DEPLOY_TARGET != ''
        run: echo deploy
